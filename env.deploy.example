# Eski namuna — kanonik: deploy/server/env.example
# Serverda: cp env.example .env

API_IMAGE=ghcr.io/mubashshir3767/currencyexchange:latest

APP_PORT=8080
ENV=PROD
RUN_MIGRATIONS=true

POSTGRES_USER=app
POSTGRES_PASSWORD=KUCHLI_PAROL
POSTGRES_DB=currency_exchange

REDIS_PW=
REDIS_DB=0
REDIS_ENABLED=true

JWTSECRET=KUCHLI_RANDOM_JWT_SECRET
JWTExpirationInSeconds=86400

FIREBASE_CREDENTIALS_PATH=
# FCM: cp docker-compose.override.example.yml docker-compose.override.yml
